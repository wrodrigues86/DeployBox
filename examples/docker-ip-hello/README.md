# Exemplo Docker por IP (sem dominio)

## Subir

```bash
docker compose up -d --build
```

## Testar no navegador

Use o IP da sua maquina + porta externa 8088:

- http://192.168.0.104:8088

## Parar

```bash
docker compose down
```
