services:
  nodepanel:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: nodepanel

    restart: unless-stopped

    ports:
      - "6000:6000"

    environment:
      NODE_ENV: production
      PORT: "4000"
